# 마일스톤

> **이 파일의 목적**: M0~M8+ 마일스톤별 진행 현황. 시간 진행에 따라 완료 표시 추가만, 본문은 거의 변경 X.
>
> **출처**: 옛 `시스템_구현_기획서.md` §7.

---

## 7. 마일스톤

### M0: 성능 스파이크 ✅ 완료 (Session 2)

빈 Phaser 씬에 per-cell Graphics N개로 `r(θ,t)` polygon을 그려 60fps 확인.
**Go/No-go 결정 지점**.

산출물:
- [x] `prototype/mesh_perf_test.html` — homo/mixed × random/y-sorted/color-grouped × 8~128 verts × 50~5000 cells 토글
- [x] 측정 결과 → §1.2에 반영
- [x] **판정: GO** (단일 기기 기준)
- [ ] Chrome throttling 환경에서 약한 기기 시뮬레이션 재측정 → 보강 후 타겟 기기 확정

### M1: 호중구 단일 ✅ 완료 (Session 3)

- [x] 호중구 1마리가 화면에서 변형하며 떠다님
- [x] DNA → Graphics 렌더링 파이프라인 완성 (`CellRenderer` 어댑터 분리)
- [x] 단위 테스트: shapeFunction (7), color (6)

산출물:
- `src/domain/{dna,color,shapeFunction}.ts`
- `src/render/{CellRenderer,GraphicsCellRenderer}.ts`
- `src/entities/WhiteCell.ts`
- `src/scenes/BloodScene.ts`

### M1.5: 충격파 인터렉션 ✅ 완료 (Session 3)

§2.2.1 의 메커닉 구현. 본래 마일스톤 계획에 없었으나 장면 1 개입 메커니즘이 명확해진 시점에 자연스럽게 들어감. M2 (접촉 반응) 의 물리 통합 워밍업 역할도 겸함.

- [x] WhiteCell 에 vx/vy + 마찰 + 화면 경계 반사
- [x] `domain/shockwave.ts` (radius/impulse/alive 함수, 단위 테스트 10)
- [x] `systems/ShockwaveSystem.ts` (자원 관리 + 파동 풀 + 임펄스 적용)
- [x] `render/ShockwaveRenderer.ts` (링 시각화)
- [x] BloodScene 호중구 10마리 + pointerdown 입력 + charges HUD
- [x] 시각 동작 확인 (사용자 검수 통과)

### M2: 세균 생태계 — **단계 분할**

#### M2.1: 단순 추적 + 분열 ✅ 완료 (Session 4)

- [x] 세균 DNA 프리셋 (`BACTERIA` → `BACTERIA_A` 로 M2.2 에서 개명)
- [x] `CellRenderHandle.setScale` 추가
- [x] `Nutrient` + `NutrientSystem` (40 슬롯, 4초 부활 지연 — active/inactive 모델)
- [x] `NutrientRenderer` (단일 Graphics batch)
- [x] `Bacteria` (DNA + 흡수 카운터 + mitosis)
- [x] `BacteriaBehaviorSystem` (단순 추적 → M2.2 에서 drives 로 확장)
- [x] BloodScene 통합 (세균 3마리, 영양분 40)
- [x] 영양분은 충격파 영향 없음 보장
- [x] 단위 테스트: nutrientSystem 8 케이스
- [ ] 백혈구 ↔ 영양분 통과 시 시각 효과 (TODO — 추후 별도 작업)

#### M2.2: drives + 관성 + 분리력 ✅ 완료 (Session 5)

- [x] DNA 스키마 확장 (`drives` 객체 + `behavior.turnRate`)
- [x] 호중구 프리셋에 무영향 기본값 추가 (drives 모두 weight=0, turnRate=0)
- [x] `domain/drives.ts` (4종 평가 함수 + `computeDesiredDirection`)
- [x] `domain/separation.ts` (같은 종족 쌍별 분리력)
- [x] BacteriaBehaviorSystem drives 호출 + turnRate 기반 부드러운 가속
- [x] BloodScene 에서 `applySeparation` 두 번 호출 (백혈구끼리, 세균끼리)
- [x] `BACTERIA` → `BACTERIA_A` 개명 (다종 세균 대비)
- [x] 단위 테스트: drives 16 케이스, separation 5 케이스

### M3: 백혈구 ↔ 세균 접촉 반응 ✅ 완료 (Session 6)

원래 계획보다 범위가 커져서 4단계로 분할. 약화/사망까지 포함하여 본 게임의 핵심 전투 루프가 완성됨.

#### M3.1: 호중구 자체 추진 ✅
- [x] `drives.seekPrey` 추가 (양 종족 동일 스키마, 호중구만 활성)
- [x] `Senses` 객체 도입 — `computeDesiredDirection` 시그니처 정리
- [x] `WhiteCellBehaviorSystem` 신규 — drives 기반 부드러운 추진
- [x] 호중구 speed=15(세균의 절반), turnRate=1.5 (옵션 C: 약한 자체 추진 + 충격파 가속)

#### M3.2: 충격파 시각 강화 ✅
- [x] `generatePolygon` 에 `ampBoost` 인자 추가
- [x] `setActivation` (당시 단일 채널) 인터페이스
- [x] WhiteCell `shockResponse` + 지수 감쇠
- [x] ShockwaveSystem 이 임펄스 크기 누적 → 시각 자극

#### M3.3: HP 시스템 ✅
- [x] `DNA.combat = { maxHp, attack }` 추가
- [x] WhiteCell/Bacteria `hp` + `applyDamage`/`hpRatio`/`isDead`
- [x] hp → 시각 스케일 매핑 (MIN_SCALE 0.5 ~ 1.0)
- [x] hp → 자체 추진 속도 매핑 (약화 시 추진력 ↓)
- [x] 분열 시 hpScale × mitosisScale 합성
- [x] HUD 평균 hp 표시

#### M3.4: 전투 + 사망 + 시체 낙하 ✅
- [x] `setVisualState({shock, combat, life})` 통합 인터페이스 — `setActivation` 폐기
- [x] GraphicsCellRenderer 3채널 합성 (§2.5 참조)
- [x] WhiteCell/Bacteria `combatResponse` + 회복
- [x] WhiteCell/Bacteria `updateAsCorpse` (중력 낙하, 회색, 행동 정지)
- [x] `domain/contact.ts` + `systems/ContactSystem.ts` (충돌 감지 + 양쪽 hp 데미지 + combat 시각 자극)
- [x] 시스템들 isDead 제외 (Behavior/Shockwave/Separation)
- [x] 단위 테스트: contact 6 케이스

### M5: 커맨더 + 팀 + 대식세포 ✅ 완료 (Session 7)

M4 (단순 시체 청소) 가 M5 의 일부로 자연스럽게 흡수됨 — 대식세포가 청소 + 호중구 재생산까지 담당.

#### M5.1: 커맨더 + 팀 영입/해체 ✅
- [x] `BACTERIA_COMMANDER` DNA 프리셋 + `DNA.command` 스키마
- [x] Bacteria 에 `level/totalAbsorbed/currentVisionRange/currentCommandRange/currentMaxTeamSize()/isCommander()`
- [x] 흡수 분기 (커맨더는 시야 ↑/레벨업, 일반은 분열)
- [x] `TeamSystem` 신규 — 영입/탈퇴/해체 + 팀별 `Team{commander, members, mode, attackTarget}`
- [x] HUD `[lvl0 N/3]` 표시

#### M5.2: 멤버 지휘범위 유지 ✅
- [x] `drives.followCommander: { weight }` 추가
- [x] `Senses.commander = { x, y, controlRadius } | null`
- [x] `evalFollowCommander` — 지휘범위 안 자유, 밖에서 끌림
- [x] BacteriaBehaviorSystem 이 senses.commander 채움
- [x] 단위 테스트: drives 5 케이스 추가 (총 25)

#### M5.3: 팀 모드 + 공격 명령 ✅
- [x] TeamSystem.decideTeamModes — 시야 안 호중구 + 우세 비교 (1.5배) + 멤버 ≥ baseTeamSize
- [x] `AGGRESSIVE_DRIVES` 상수 (회피 ↓, seekPrey ↑, followCommander 0)
- [x] BacteriaBehaviorSystem override 분기 — **멤버만**, 커맨더는 자기 drives 유지 (지휘 역할)
- [x] 호중구 사망 시 팀 보상 (영양분 1개 흡수와 동등 — `registerAbsorb` 팀 전체)
- [x] 시야 분리 (회피 180 / 공격 결정 540)

#### M5.4: 대식세포 + 호중구 재생산 ✅
- [x] `MACROPHAGE` DNA 프리셋 + `Macrophage` 엔티티
- [x] `MacrophageSystem` — 침전 시체 X 추격 + 흡수 + 점수 (세균 10, 호중구 20)
- [x] `WhiteCell.isSettled() / isAbsorbed`, Bacteria 동일
- [x] BehaviorSystem 들의 `removeAbsorbed()`
- [x] `NEUTROPHIL_SUPER` 프리셋 (base 40, hp 180, attack 30, speed 32, 진한 보라)
- [x] BloodScene `tryProduceWhiteCell` — 100점 도달 시 호중구 1마리, 호중구 점수 ≥ 40 면 슈퍼

### M6: 100개 부하 테스트 ✅ 완료 (Session 9)

- [x] 디버그 환경 (FPS HUD + 키 토글)
- [x] **MVP 성공 기준 달성** — 호중구 100 + 세균 50 + 분열 = 60fps 안정
- [x] 모든 시스템 (전투/팀/대식세포/충격파) 활성 상태에서도 끊김 없음
- [x] D 방식(SDF) 도입 불필요 — 현재 per-cell Graphics 충분
- [ ] **균형 미세조정 (잔여)**: 사체 누적 → 호중구 과잉 생산 문제. 후속 작업 후보:
  - 점수 임계 ↑ (100 → 150)
  - 호중구 사체 페이드아웃 (대식세포 흡수 전 자동 소멸)
  - 영양분 수 ↑ (40 → 60)

### M7: 메커닉 확장 (진행 중) — Session 10

- [x] 호중구 분리 효과 (대식세포 위치 + 위 임펄스)
- [x] 커맨더 사망 후 10초 진화 (§2.7)
- [x] **NK 세포 추가** — 1/10 확률 생산, 커맨더 우선 추적, 일반 세균 회피, HP 무관 속도
- [x] drives 8개로 확장 (`seekCommander`, `avoidWorker`)
- [x] behavior `minSpeedRatio` 추가 (HP 영향 캡)
- [x] **호중구 자기 융합** — 약한 동료 흡수, mergeCounter 2 → 슈퍼 호중구 (§2.6.1, 두 번째 생산 경로)
- [x] **빨리감기** — 가상 시간 + speedMultiplier (§2.8, [1/2/3] = 1/2/4x)
- [x] **B세포 + 항체 발사체** — 미사일 기지형, 지뢰형 발사체, 세균 흡수 시 HP 감소 (§2.9)
- [x] B세포 항체 5개 제한 + 발사체 속도 1/2 (Session 12 미세조정)
- [x] **T세포 + 호중구 진화** — 휘하 호중구 level 5 → NK/BCELL/SUPER 무작위 진화, HP 회복 (§2.10)
- [ ] 균형 조정 (사체 누적 → 호중구 과잉)
- [ ] 다종 세균 (군집형 BACTERIA_B 등 — seekAlly 활성)
- [ ] T세포 명령 메커니즘 (시야 안 표적 override 등)
- [ ] T세포 / 세균 커맨더 복수화
- [ ] 다른 백혈구 종 (호산구 등 — 아이디어 기획서 §백혈구)

### M8+: 후속 후보

- 시각 디테일 (백혈구 ↔ 영양분 통과 효과)
- BloodScene 분리 (SimulationPipeline 패턴)
- 장면 2 (세포 속 미니게임 — 아이디어 기획서 §장면 2 참조)
- 타겟 기기 검증 (Chrome throttling)

---

