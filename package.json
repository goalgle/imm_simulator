{
  "name": "immune-simulator",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "함수 기반 절차적 형태로 표현되는 면역 시뮬레이션 게임",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "dependencies": {
    "phaser": "^3.80.1",
    "tone": "^15.1.22"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "typescript": "^5.5.4",
    "vite": "^5.4.0",
    "vitest": "^2.0.0"
  }
}
